(executable
 (name main)
 (public_name bearbeer)
 (libraries bearbeer dream tyxml))

(rule
 (with-stdout-to
  theme.ml
  (progn
   (echo "let style_css = {|")
   (cat resources/theme.css)
   (echo "|}"))))
