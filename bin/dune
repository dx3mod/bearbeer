(executable
 (name main)
 (public_name bearbeer)
 (libraries bearbeer dream tyxml cmdliner))

(env
 (_
  (flags
   (:standard -open Containers))))

(rule
 (with-stdout-to
  resource_style_css.ml
  (progn
   (echo "let plain = {|")
   (cat resources/style.css)
   (echo "|}"))))

(rule
 (with-stdout-to
  resource_github_theme.ml
  (progn
   (echo "let plain = {|")
   (cat resources/theme.github.css)
   (echo "|}"))))
